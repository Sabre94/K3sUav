---
apiVersion: v1
kind: Namespace
metadata:
  name: uav-system
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: uav-router
  namespace: uav-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: uav-router
rules:
  # UAVMetrics CRD 权限
  - apiGroups: ["uav.k3s.io"]
    resources: ["uavmetrics"]
    verbs: ["get", "list", "watch"]

  # Pod 权限（用于 endpoint 发现）
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "list", "watch"]

  # Endpoints 权限
  - apiGroups: [""]
    resources: ["endpoints"]
    verbs: ["get", "list", "watch"]

  # Service 权限
  - apiGroups: [""]
    resources: ["services"]
    verbs: ["get", "list", "watch"]

  # Node 权限（获取节点信息）
  - apiGroups: [""]
    resources: ["nodes"]
    verbs: ["get", "list"]

  # Istio DestinationRule 权限
  - apiGroups: ["networking.istio.io"]
    resources: ["destinationrules"]
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: uav-router
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: uav-router
subjects:
  - kind: ServiceAccount
    name: uav-router
    namespace: uav-system
---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: uav-router
  namespace: uav-system
  labels:
    app: uav-router
spec:
  selector:
    matchLabels:
      app: uav-router
  template:
    metadata:
      labels:
        app: uav-router
    spec:
      serviceAccountName: uav-router
      hostNetwork: true  # 使用宿主机网络，便于与 Ztunnel 通信
      containers:
        - name: router
          image: x1224403599/uav-router:v0.2.0-istio
          imagePullPolicy: IfNotPresent
          env:
            # 节点名称（从 downward API 获取）
            - name: NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName

            # 路由算法选择
            - name: ALGORITHM
              value: "distance-based"  # 可选: distance-based, battery-aware, composite

            # API 端口
            - name: API_PORT
              value: "8080"

            # Istio 集成配置
            - name: ENABLE_ISTIO
              value: "true"

            # Istio 最小权重阈值（权重低于此值的端点将被过滤）
            - name: ISTIO_MIN_WEIGHT
              value: "10"

            # Istio 权重变化百分比阈值（变化超过此值才触发更新）
            - name: ISTIO_MIN_CHANGE_PERCENT
              value: "5.0"

            # Istio 启用调试注释
            - name: ISTIO_ENABLE_ANNOTATIONS
              value: "true"

            # Istio DestinationRule 名称前缀
            - name: ISTIO_NAME_PREFIX
              value: "uav-"

          ports:
            - name: http
              containerPort: 8080
              protocol: TCP

          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi

          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10

          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5

      # 只在工作节点上运行（不在 master 节点）
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: node-role.kubernetes.io/control-plane
                    operator: DoesNotExist
---
# Service 用于访问 Router Agent API（可选）
apiVersion: v1
kind: Service
metadata:
  name: uav-router
  namespace: uav-system
  labels:
    app: uav-router
spec:
  type: ClusterIP
  clusterIP: None  # Headless service
  selector:
    app: uav-router
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
