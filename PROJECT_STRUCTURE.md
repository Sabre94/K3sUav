# K3sUav 项目结构

完整的 UAV 监控和智能调度系统

## 📊 项目概览

```
K3sUav/
├── 📄 文档
│   ├── README.md                    # 项目主文档（Agent 部分）
│   ├── SCHEDULER.md                 # 调度器完整文档
│   ├── QUICKSTART_SCHEDULER.md      # 调度器快速入门
│   ├── DEPLOY.md                    # 部署指南
│   └── PROJECT_STRUCTURE.md         # 本文档
│
├── 🔧 配置文件
│   ├── go.mod                       # Go 模块依赖
│   ├── go.sum                       # 依赖校验
│   ├── Dockerfile                   # Agent 镜像构建
│   ├── Dockerfile.scheduler         # Scheduler 镜像构建
│   └── Makefile                     # 构建和部署命令
│
├── 📦 API 定义
│   └── api/
│       └── crd/
│           └── uav-metrics-crd.yaml # UAVMetrics CRD 定义
│
├── 🚀 命令行程序
│   └── cmd/
│       ├── agent/
│       │   └── main.go              # Agent 主程序
│       └── scheduler/
│           └── main.go              # Scheduler 主程序
│
├── 📚 核心包
│   └── pkg/
│       ├── config/
│       │   └── config.go            # Agent 配置管理
│       │
│       ├── k8s/
│       │   └── client.go            # K8s 客户端封装
│       │
│       ├── collector/
│       │   └── collector.go         # 数据采集器
│       │
│       ├── models/
│       │   ├── types.go             # 数据模型
│       │   └── errors.go            # 错误定义
│       │
│       └── scheduler/               # 🔌 调度器包（新增）
│           ├── scheduler.go         # 核心调度逻辑
│           │
│           ├── algorithm/           # 可插拔算法
│           │   ├── interface.go     # 算法接口定义
│           │   ├── distance_based.go    # 基于距离
│           │   ├── battery_aware.go     # 基于电池
│           │   ├── network_latency.go   # 基于网络
│           │   └── composite.go         # 组合算法
│           │
│           ├── registry/
│           │   └── registry.go      # 算法注册表
│           │
│           └── config/
│               └── config.go        # Scheduler 配置
│
├── 🚢 部署文件
│   └── deploy/
│       ├── agent-daemonset.yaml     # Agent DaemonSet + RBAC
│       ├── scheduler-deployment.yaml # Scheduler Deployment + RBAC
│       └── deploy.sh                # 自动部署脚本
│
├── 📝 示例
│   └── examples/
│       └── test-pod.yaml            # 测试 Pod 示例
│
└── 🔨 编译产物
    └── bin/
        ├── uav-agent                # Agent 二进制（15MB）
        └── uav-scheduler            # Scheduler 二进制（35MB）
```

## 🎯 两大核心模块

### 1️⃣ UAV Agent（数据采集）

**功能**：在每个节点上收集 UAV 遥测数据并存储到 CRD

**部署方式**：DaemonSet（每节点一个 Pod）

**数据采集**：
- GPS 位置（纬度、经度、高度、速度）
- 电池状态（电量、电压、温度）
- 飞行数据（模式、姿态、高度）
- 网络状态（延迟、带宽、信号）
- 系统性能（CPU、内存、磁盘）
- 健康检查（状态、错误、警告）

**关键文件**：
- `cmd/agent/main.go` - 主程序
- `pkg/collector/collector.go` - 数据采集逻辑
- `pkg/k8s/client.go` - CRD 操作
- `deploy/agent-daemonset.yaml` - 部署配置

### 2️⃣ UAV Scheduler（智能调度）

**功能**：根据 UAVMetrics 数据智能调度 Pod 到最合适的节点

**部署方式**：Deployment（单副本）

**核心特性**：
- 🔌 **可插拔算法架构**
- 📊 **实时 CRD 数据**
- 🎯 **多种内置算法**
- 🔧 **易于扩展**

**内置算法**：
1. **Distance-based** - 基于 GPS 距离
2. **Battery-aware** - 基于电池电量
3. **Network-latency** - 基于网络延迟
4. **Composite** - 组合多个算法

**关键文件**：
- `cmd/scheduler/main.go` - 主程序
- `pkg/scheduler/scheduler.go` - 调度核心逻辑
- `pkg/scheduler/algorithm/*.go` - 算法实现
- `pkg/scheduler/registry/registry.go` - 算法注册
- `deploy/scheduler-deployment.yaml` - 部署配置

## 🔄 数据流

```
┌─────────────────────────────────────────────────────────────┐
│                    K3s 集群                                  │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  节点 1                                               │  │
│  │  ┌────────────┐                                      │  │
│  │  │ UAV Agent  │ ──► UAVMetrics CRD                   │  │
│  │  └────────────┘     (GPS, Battery, etc.)             │  │
│  └──────────────────────────────────────────────────────┘  │
│                               │                             │
│  ┌────────────────────────────┼─────────────────────────┐  │
│  │  节点 2                     │                         │  │
│  │  ┌────────────┐            │                         │  │
│  │  │ UAV Agent  │ ────────────┘                         │  │
│  │  └────────────┘                                       │  │
│  └──────────────────────────────────────────────────────┘  │
│                               │                             │
│                               ▼                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │           Kubernetes API Server                       │  │
│  │           (etcd: UAVMetrics CRDs)                     │  │
│  └──────────────────┬───────────────────────────────────┘  │
│                     │                                       │
│                     │ Watch & Read                          │
│                     │                                       │
│  ┌──────────────────▼───────────────────────────────────┐  │
│  │  UAV Scheduler                                        │  │
│  │  ┌────────────────────────────────────────────────┐  │  │
│  │  │ 1. Watch 未调度的 Pod                           │  │  │
│  │  │ 2. 读取 UAVMetrics                              │  │  │
│  │  │ 3. 执行算法评分                                  │  │  │
│  │  │ 4. 选择最佳节点                                  │  │  │
│  │  │ 5. 绑定 Pod                                      │  │  │
│  │  └────────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Pod (schedulerName: uav-scheduler)                   │  │
│  │  ──► 被调度到最合适的节点                             │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

## 🛠️ 构建命令

### Agent 相关
```bash
make build                # 编译 Agent
make build-image          # 构建 Agent 镜像
make deploy               # 部署 Agent
make status               # 查看 Agent 状态
make logs                 # 查看 Agent 日志
make clean                # 清理 Agent
```

### Scheduler 相关
```bash
make build-scheduler           # 编译 Scheduler
make build-scheduler-image     # 构建 Scheduler 镜像
make deploy-scheduler          # 部署 Scheduler
make scheduler-status          # 查看 Scheduler 状态
make scheduler-logs            # 查看 Scheduler 日志
make test-scheduler            # 本地测试 Scheduler
make clean-scheduler           # 清理 Scheduler
```

### 通用
```bash
make help                 # 查看所有命令
make deploy-crd           # 部署 CRD
make clean-all            # 完全清理（包括 CRD）
```

## 📈 开发流程

### 添加新的调度算法

1. **创建算法文件**：`pkg/scheduler/algorithm/my_algo.go`
   ```go
   package algorithm

   type MyAlgorithm struct {
       // 参数
   }

   func (a *MyAlgorithm) Name() string {
       return "my-algorithm"
   }

   func (a *MyAlgorithm) Score(...) ([]NodeScore, error) {
       // 评分逻辑
   }
   ```

2. **注册算法**：在 `cmd/scheduler/main.go` 中
   ```go
   myAlgo := algorithm.NewMyAlgorithm()
   registry.Register(myAlgo)
   ```

3. **使用算法**：
   ```bash
   export ALGORITHM_NAME=my-algorithm
   ./bin/uav-scheduler
   ```

### 添加新的数据采集

1. **扩展数据模型**：`pkg/models/types.go`
   ```go
   type UAVMetrics struct {
       // ... 现有字段
       MyNewData *MyNewData `json:"myNewData,omitempty"`
   }
   ```

2. **实现采集逻辑**：`pkg/collector/collector.go`
   ```go
   func (c *Collector) collectMyNewData(ctx context.Context) (*models.MyNewData, error) {
       // 采集逻辑
   }
   ```

3. **更新 CRD**：`api/crd/uav-metrics-crd.yaml`
   ```yaml
   myNewData:
     type: object
     properties:
       # 字段定义
   ```

## 🧪 测试流程

### 1. 本地测试 Agent
```bash
export NODE_NAME=$(hostname)
export KUBECONFIG=/etc/rancher/k3s/k3s.yaml
./bin/uav-agent
```

### 2. 本地测试 Scheduler
```bash
export KUBECONFIG=/etc/rancher/k3s/k3s.yaml
export ALGORITHM_NAME=distance-based
export LOG_LEVEL=debug
./bin/uav-scheduler
```

### 3. 创建测试 Pod
```bash
kubectl apply -f examples/test-pod.yaml
kubectl get pod test-uav-scheduled-pod -o wide
```

### 4. 查看调度日志
```bash
kubectl logs -l app=uav-scheduler -f
```

## 📊 性能指标

### Agent
- 数据采集：0-6ms
- CRD 更新：17-22ms
- 总处理时间：30-40ms
- 内存占用：~30MB
- CPU 占用：<5%

### Scheduler
- 调度延迟：30-100ms
- 内存占用：~50MB
- CPU 占用：<10%

## 🔐 RBAC 权限

### Agent 需要的权限
- UAVMetrics CRD: get, list, create, update, patch
- UAVMetrics/status: update, patch
- Nodes: get, list（可选）

### Scheduler 需要的权限
- Pods: get, list, watch
- Pods/binding: create
- Pods/status: patch, update
- Nodes: get, list, watch
- UAVMetrics CRD: get, list, watch
- Events: create, patch

## 📚 文档索引

- **[README.md](./README.md)** - 项目主文档（Agent）
- **[SCHEDULER.md](./SCHEDULER.md)** - 调度器完整文档
- **[QUICKSTART_SCHEDULER.md](./QUICKSTART_SCHEDULER.md)** - 5分钟快速入门
- **[DEPLOY.md](./DEPLOY.md)** - 详细部署指南
- **[PROJECT_STRUCTURE.md](./PROJECT_STRUCTURE.md)** - 本文档

## 🚧 未来规划

### Agent 增强
- [ ] 集成真实 GPS 硬件
- [ ] 集成真实飞控系统（MAVLink）
- [ ] Prometheus metrics 导出
- [ ] 单元测试（目标 >70%）

### Scheduler 增强
- [ ] 更多算法（负载均衡、能耗优化）
- [ ] 算法性能分析和可视化
- [ ] 支持 Webhook 动态加载算法
- [ ] 多调度器协作
- [ ] 调度模拟和预测

### 系统集成
- [ ] Web UI 可视化
- [ ] Istio Ambient 路由集成
- [ ] LLM 智能决策
- [ ] 多集群支持

## 📄 许可证

MIT License

---

**版本**: v0.1.0
**更新时间**: 2025-11-06
**作者**: K3sUav Team
